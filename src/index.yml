openapi: "3.0.0"
info:
  version: 0.1.0
  title: "Example Application API Specs"
  license:
    name: example.com.
servers:
  - url: http://localhost
    description: Local environment
paths:
  $ref: './paths/index.yml'
components:
  $ref: './components/index.yml'
security:
  - bearerAuth: []
